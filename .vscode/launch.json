{
    "configurations": [
        {
            "name": "Launch via NPM",
            "request": "launch",
            "type": "node-terminal",
            "command": "npm run start:dev"
        }
    ]
}